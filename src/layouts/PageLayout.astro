---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import MessengerFab from '../components/MessengerFab.astro';
import AIAssistant from '../components/AIAssistant.astro';
import StickyMobileCTA from '../components/StickyMobileCTA.astro';

interface Props {
  title: string;
  description: string;
  image?: string;
  noindex?: boolean;
  alternateUk?: string;
  alternateRu?: string;
  ogImage?: string;
}

const { title, description, image, noindex, alternateUk, alternateRu, ogImage } = Astro.props;
---

<BaseLayout title={title} description={description} image={image} noindex={noindex} alternateUk={alternateUk} alternateRu={alternateRu} ogImage={ogImage}>
  <Header />
  <main id="main">
    <slot />
  </main>
  <Footer />
  <MessengerFab />
  <AIAssistant />
  <StickyMobileCTA />
</BaseLayout>
